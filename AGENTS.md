## Imported Claude Cowork project instructions

## Entrega e deploy

- Sempre incluir, ao concluir alteracoes, os comandos exatos para enviar as mudancas pelo PowerShell no Windows (`git add`, `git commit` e `git push`) e para atualizar/reiniciar a aplicacao no servidor Linux (`git pull` e os comandos do processo em uso).
- O ambiente de teste atual roda a central no Linux dentro de `screen`, usando `npm run dev`; incluir tambem como encerrar/reiniciar essa sessao e como acompanhar o log de inicializacao.
