# Evita compilar dependências nativas OPCIONAIS (ex.: cpu-features do ssh2), que
# em alguns servidores/VPS virtualizados causam "Segmentation fault" ao carregar.
# O ssh2 funciona 100% em modo JavaScript puro sem elas (só um pouco mais lento).
omit=optional
